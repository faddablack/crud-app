# TODO App
This is a todo app with local persistence using SQLite across browser sessions. 

## To Do
- [x] Create checkbox component 
- [x] Add checkbox that will mark task as done, this should appear at the front of each task name.
- [x] Use SQLite as a database to persist the saved tasks across different browser sessions.
- [x] Add user feedback that will alert the user when they're about to delete a task.
- [x] Add timestamp to track when tasks were created.
- [x] Display timestamp next to new tasks.
- Update timestamp to capture when the task was edited.
-  Add search bar for searching tasks.
